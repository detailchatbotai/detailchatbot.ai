[deploy]
startCommand = "pip install -r backend/requirements.txt && PYTHONPATH=/app/backend alembic -c backend/alembic.ini upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10